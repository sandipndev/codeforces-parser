#include <bits/stdc++.h>
using namespace std;

#define DEBUG

void run() {
    int N;
    cin >> N;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int T;
    cin >> T;
    for (int i = 0; i < T; i++) run();
    return 0;
}